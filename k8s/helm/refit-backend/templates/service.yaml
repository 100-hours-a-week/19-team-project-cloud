apiVersion: v1
kind: Service
metadata:
  name: refit-backend-svc
  namespace: refit-app
spec:
  selector:
    app: refit-backend
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
